---
// Базовый layout для всех страниц ghspictograms.com
import '../styles/global.css'

interface Props {
  title: string
  description?: string
  canonical?: string
}

const {
  title,
  description = 'GHS pictograms, label builder and visual chemical safety tools for design, logistics and compliance.',
  canonical
} = Astro.props

const siteUrl = 'https://ghspictograms.com'
const canonicalUrl = canonical ?? `${siteUrl}${Astro.url.pathname}`

/** Текущий сайт в экосистеме — всегда ghspictograms в этом layout */
const ecosystem = [
  { label: 'GHS Symbols', href: 'https://ghssymbols.com', id: 'symbols' as const },
  { label: 'GHS Pictograms', href: 'https://ghspictograms.com', id: 'pictograms' as const },
  { label: 'GHS Labels', href: 'https://ghslabels.com', id: 'labels' as const },
]
const activeId = 'pictograms'
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <title>{title} | GHS Pictograms</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet">

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Open Graph -->
    <meta property="og:title" content={`${title} | GHS Pictograms`} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:type" content="website" />
    <slot name="head" />
    <!-- Google Analytics 4 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-1Y5MKV5XMK"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-1Y5MKV5XMK');
    </script>
  </head>
  <body class="bg-white text-gray-900 font-sans antialiased">
    <header class="bg-[#062A78] sticky top-0 z-50">
      <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between gap-4">
        <a href="/" class="flex items-center gap-2 font-bold text-xl text-white shrink-0">
          GHS Pictograms
        </a>

        <ul class="hidden md:flex items-center gap-1 lg:gap-2 text-sm font-medium">
          {ecosystem.map(({ label, href, id }) => (
            <li>
              <a
                href={href}
                class:list={[
                  'inline-block rounded-md px-3 py-1.5 transition-colors',
                  id === activeId
                    ? 'bg-white text-[#062A78] font-semibold shadow-sm'
                    : 'text-white hover:bg-white/10',
                ]}
              >
                {label}
              </a>
            </li>
          ))}
        </ul>

        <details class="md:hidden relative group">
          <summary class="list-none cursor-pointer p-2 text-white hover:bg-white/10 rounded-lg transition-colors [&::-webkit-details-marker]:hidden">
            <span class="sr-only">Open menu</span>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </summary>
          <ul class="absolute right-0 top-full mt-1 w-56 bg-[#062A78] border border-blue-700 rounded-xl shadow-xl py-2 text-sm font-medium">
            {ecosystem.map(({ label, href, id }) => (
              <li>
                <a
                  href={href}
                  class:list={[
                    'block px-4 py-3 transition-colors',
                    id === activeId
                      ? 'bg-white text-[#062A78] font-semibold mx-2 rounded-md'
                      : 'text-blue-100 hover:bg-blue-800 hover:text-white',
                  ]}
                >
                  {label}
                </a>
              </li>
            ))}
          </ul>
        </details>
      </nav>
    </header>

    <main>
      <slot />
    </main>

    <footer class="bg-[#062A78] mt-24 py-12">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-sm text-blue-200 text-center">
        <p class="text-white font-semibold mb-2">GHS Pictograms</p>
        <p>Visual GHS tools for labels, logistics and compliance.</p>
        <p class="mt-4 text-blue-300">© 2026 GHS Pictograms</p>
      </div>
    </footer>
  </body>
</html>
